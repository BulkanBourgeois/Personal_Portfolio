# Blender source files

Put your `.blend` files here (e.g. `room_scene.blend`, `tv_model.blend`).

- Exported runtime assets should live in `static/models/` (GLB/glTF) and `static/textures/` (lightmaps/atlases).
- If you want a staging area before copying into `static/`, use `blender/exported/`.

